/**
 * node-editor — public API. The app shell mounts the side panel beside the
 * tree-canvas; it reacts to selection via brain and edits through BookService.
 */
export { NodeEditorPanel } from './components/NodeEditorPanel'
