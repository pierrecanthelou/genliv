/**
 * outline-view — public API. The editor shell mounts OutlineView as the
 * « plan » body, swapping it with the canvas via the view-mode switch.
 */
export { OutlineView } from './components/OutlineView'
