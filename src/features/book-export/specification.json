{
	"feature": "book-export",
	"created_at": "2026-06-20",
	"status": "in-progress",
	"design_reference": "Editeur Livre-Jeu - Wireframes.dc.html § 02 editor top bar (« Aperçu du jeu ▷ » cluster). The « Exporter le jeu ⬇ » control sits in the right cluster before the disabled preview button. Low-fi, light theme; mono control like its siblings.",
	"plan": {
		"goal": "Export the authored book to a self-contained, versioned play-ready file (download) so a future play runtime can consume it without the editor.",
		"acceptance_criteria": [
			"A « Exporter le jeu » control in the editor top bar downloads a JSON file named from the book title (slugified, .jeu.json).",
			"The exported document is versioned (format 'genliv-play', version 1) and self-contained: book identity, all nodes (minus editor-only position), all edges INCLUDING the config-derived automatic edges (échec→Mort, KR-067), and the resolved acquirable-object catalog (collectObjects).",
			"Export is a PURE transform over the BookService SSOT (no mutation, no private copy) and never blocks: dangling references (deleted targets/objects, unconfigured rules) are surfaced as structured warnings in the file and counted for the author, never silently dropped (KR-021).",
			"After a successful export a transient status appears beside the button — « ✓ Export réussi » or « ⚠ N avertissement(s) » — and book:exported is emitted with the warning count.",
			"The download helper revokes its object URL after triggering (no leak) and is a no-op outside a DOM; the status timer is ref-tracked and cleared on unmount.",
			"The shared EditorTopBar stays feature-agnostic: book-export mounts via a generic actions slot, neither feature importing the other."
		],
		"brain_contracts": [
			{ "type": "service", "name": "BookService", "direction": "consumes" },
			{ "type": "util", "name": "exportBookForPlay", "direction": "consumes" },
			{ "type": "util", "name": "downloadJson / slugifyFilename", "direction": "consumes" },
			{
				"type": "event",
				"name": "book:exported",
				"direction": "emits",
				"payload": { "bookId": "string", "warnings": "number" }
			}
		],
		"walking_skeleton": "A pure brain transform exportBookForPlay(book) builds the versioned play document (nodes minus position + authored edges + derived automatic edges + object catalog + dangling-reference warnings). A brain downloadJson util serialises + downloads it (Blob/objectURL/anchor, always revokes). The book-export feature's ExportGameButton (mounted into EditorTopBar's actions slot by the editor shell) reads the book from BookService, downloads the file, emits book:exported, and shows a transient success/warning status.",
		"n": 1,
		"iterations": [
			{ "id": 1, "goal": "Image upload (portrait/illustration), scenario import/export, AI prompt download", "status": "done" }
		],
		"known_risks": [
			"KR-119 — Play export is a PURE transform over the book SSOT (KR-020), baking the config-derived automatic edges (KR-067) + the resolved object catalog into one versioned, self-contained document; the download (Blob/objectURL/anchor) is a separate browser side-effect that ALWAYS revokes its object URL. Export is non-blocking: dangling references (KR-021) become file warnings + a count, never block or silently drop.",
			"KR-120 — Shared editor chrome (EditorTopBar) stays feature-agnostic via a generic `actions` ReactNode slot the editor shell fills with feature buttons; the bar never imports a feature (Open/Closed; mirrors the SlotRegistry seam, KR-066/109).",
			"KR-021 — References are by stable id; a target/object that no longer resolves must be surfaced, not silently broken — the export validates edge targets, prereq objects, countdown fallbacks, monster/PNJ targets, and décor object refs.",
			"Timer-safety — the transient status timeout is ref-tracked and cleared on unmount + before re-scheduling.",
			"KR-143 — importBook always mints a fresh book id (no collision); validates format marker + version + required fields + all node kinds before accepting. Returns null on any failure; callers guard the null."
		]
	},
	"implementation": {
		"iterations_log": [
			{
				"id": 0,
				"label": "walking-skeleton",
				"completed_at": "2026-06-20",
				"architecture_choices": [
					"exportBookForPlay is a pure brain util (brain/utils/playExport.ts) over the book — it reuses deriveAutomaticEdges (fatal→Mort, KR-067) and collectObjects (catalog, KR-062) rather than recomputing, so the export can never disagree with the canvas/outline views. Nodes drop the editor-only `position` (KR-022/023); everything else is play data verbatim.",
					"Referential-integrity report (KR-021): collectWarnings validates every by-id reference the domain carries — edge targets, hidden-prereq objects + countdown fallbacks (choice-linking), monster victoire/fuite + PNJ target/ref, and décor objectRef — returning structured { code, message, nodeId?, edgeId?, ref? } warnings. Non-blocking (user decision): the export always succeeds and reports the count.",
					"Download is a separate browser side-effect util (brain/utils/download.ts): Blob + object URL + synthetic anchor click, the URL ALWAYS revoked in a finally (no leak), a no-op when there is no document. slugifyFilename derives a safe .jeu.json name from the title.",
					"EditorTopBar gained a generic `actions` ReactNode slot (KR-120) filled by the editor shell with <ExportGameButton>; the bar never imports the feature. The button owns a transient post-export status (timer ref-tracked + cleared on unmount, timer-safety rule) and emits book:exported."
				],
				"deviations_from_plan": [
					"New capability outside the ragged-iteration plan (like the choice-linking lineage refinement) — a single walking-skeleton slice (n=0), not part of a feature tier. Play mode itself stays deferred; this only produces the file it will consume."
				],
				"test_coverage": {
					"unit": "exportBookForPlay (format/version, position-stripped nodes, derived fatal edge folded in, catalog, each dangling-reference warning) + download (slugify cases, blob/anchor/revoke, revoke-on-throw)",
					"integration": "ExportGameButton via BrainProvider — clean export shows success + triggers the download; a dangling prereq shows « 1 avertissement » + emits book:exported{warnings:1}; a missing book is a no-op",
					"e2e": "deferred (Playwright not set up)"
				}
			},
			{
				"id": 1,
				"label": "image-upload-scenario-io-ai-prompt",
				"completed_at": "2026-06-24",
				"architecture_choices": [
					"ImageUpload is a new brain/components primitive (FileReader.readAsDataURL → data URL via onChange prop) consumed by PnjEditor (portrait) and NodeEditorPanel (illustration); stored directly on PnjConfig.portrait and BookNode.illustration as optional strings.",
					"ScenarioExport type + exportScenario/isScenarioExport live in brain/utils/scenarioExport.ts (single source). BookService.importBook validates with isScenarioExport + hasOnlyKnownKinds, always mints a fresh book id (KR-143), emits book:created.",
					"ImportScenarioButton (book-export feature) injected into LibraryScreen via an optional importEntry ReactNode prop on LibraryScreen; App.tsx is the composition root — no cross-feature import.",
					"DownloadAiPromptButton serves /PROMPT_SCENE_IA.md from public/ via a plain anchor (no Blob, no ?raw import) to avoid tsconfig vite/client issues.",
					"downloadText added to brain/utils/download.ts alongside downloadJson; both share the Blob+anchor pattern."
				],
				"deviations_from_plan": [
					"Plan described only the play-export skeleton. This iteration extends the feature with three additional capabilities: scenario import/export, image upload, and AI prompt download — all cross-cutting but owned by this feature slot."
				],
				"test_coverage": {
					"unit": "ImageUpload (4 tests: empty state, preview+remove, remove callback, FileReader stub); exportScenario + isScenarioExport (5+1 tests); BookService.importBook (3 tests: fresh id, null rejection, book:created event)",
					"integration": "pnj.test.tsx updated: portrait affordance now checks for ImageUpload dropzone button instead of deferred placeholder",
					"e2e": "deferred"
				}
			}
		],
		"open_questions": [
			"Library-card export action (the second placement option) — deferred; the shared useExportBook hook + pure transform already make it a thin add when wanted.",
			"When play mode lands, it consumes this format; bump PLAY_EXPORT_VERSION + add a reader if the schema changes."
		]
	}
}
