/**
 * dossier-canon — public API. Only the Canon panel is exposed; injected by the
 * composition root (App.tsx) into bascule-editeur's DossierEditorScreen slot,
 * never imported directly by another feature.
 */
export { PanneauCanon } from './components/PanneauCanon'
