/**
 * dossier-objets — public API. Only the section panel is exposed; injected by
 * the composition root (App.tsx) into bascule-editeur's DossierEditorScreen
 * slot, never imported directly by another feature.
 */
export { PanneauObjets } from './components/PanneauObjets'
