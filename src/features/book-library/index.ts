/**
 * book-library — public API. The app shell mounts LibraryScreen as the home
 * route, injecting the create affordance so book-creation and book-library
 * never import each other.
 */
export { LibraryScreen } from './components/LibraryScreen'
