/**
 * dossier-controles — public API. Only the panel is exposed; injected by the
 * composition root (App.tsx) into bascule-editeur's DossierEditorScreen
 * `panneauControles` slot, never imported directly by another feature.
 */
export { PanneauControles } from './components/PanneauControles'
