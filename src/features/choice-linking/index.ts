/**
 * choice-linking — public API. The composition root calls registerChoiceLinking
 * to mount the outgoing-choices UI into node-editor's slot via brain.
 */
export { registerChoiceLinking } from './register'
