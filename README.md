# genliv — Éditeur de « livre dont vous êtes le héros »

Authoring tool for gamebooks (choose-your-own-adventure). UI and domain terms are in **French**.

**🔀 Bascule en cours** : le produit passe d'un **arbre de choix** à un **dossier d'aventure joué par une IA**. Le plan exécutable — décisions tranchées, ordre des features, ce qui a été supprimé — est [`docs/ROADMAP-BASCULE-IA.md`](./docs/ROADMAP-BASCULE-IA.md). Temps 1 : l'éditeur produit un dossier. Temps 2 : le moteur le joue.

Règles du jeu (source de vérité) : [`docs/REGLES-DU-JEU.md`](./docs/REGLES-DU-JEU.md). Design handoff : [`design_handoff_gamebook_editor/`](./design_handoff_gamebook_editor). Règles toujours actives : [`CLAUDE.md`](./CLAUDE.md).

## Stack

- **React 18 + TypeScript**, built with **Vite**.
- **Jest + React Testing Library** for unit/component tests (Playwright E2E planned).
- **ESLint + Prettier** (tabs, single quotes, no semicolons).

## Commands

```bash
npm install      # install dependencies
npm run dev      # Vite dev server (http://localhost:5173)
npm run build    # type-check + production build
npm test         # Jest unit/component tests
npm run lint     # ESLint
npm run typecheck# tsc --noEmit
```

## Architecture

```
src/
  main.tsx          # React root: wires brain + features
  App.tsx           # shell: maps route -> view
  style.css         # reset + design-system tokens (styles/)
  styles/           # design system: styles.css + tokens/*.css (colors, type, spacing, fonts)
  brain/            # core engine — feature-agnostic, never imports features/
    types.ts            # rules types (GameObject, SkillRoll, MonsterConfig, …) — consumed by player/
    tree.ts             # the tree model (Book, BookNode, Edge) — condemned: n° 2 then n° 9
    dossier/            # n° 1 — the adventure dossier: schema 1, validator, issues, deep freeze
    DossierService.ts   # get / open / importDossier / exportDossier (get re-validates, never raw)
    EventBus.ts         # Observer: typed cross-feature events
    Router.ts           # navigation contract
    PersistenceService.ts + persistenceKeys.ts   # the only storage gateway (KR-011/111)
    BookService.ts      # single source of truth for the book tree; seed factory
    BrainContext.tsx    # DI via React Context (Service Locator)
    components/         # cross-feature presentational primitives (KR-109)
    utils/              # cross-feature utilities (KR-110), e.g. stable id generation
  features/         # isolated feature modules; talk to the rest only through brain/
    dossier-format/     # n° 1 — import an adventure dossier + read its rejection report
    bascule-editeur/    # n° 2 — 3/3 done: DossierEditorScreen, two-column ten-section nav (SECTIONS, ListRow)
    dossier-canon/      # n° 3 — 4/4 done: Canon + Depart + Objectifs + Lieux panels via DossierService.update()
    dossier-fiches/     # n° 4 — 8/8 TERMINÉE: PanneauPersonnages + FichePersonnage + Accordion (8/8 slots filled) + retrait (modale, refus SSOT): name, camp, plan, objectif_id, identity prose, stats (8 keys, total-when-present) + derived PV, but + plan_actions (duree/si_bloque) + contre_mesures, relations (cible/lien/intensite/secret) + presence (lieu/quand), savoirs (indice_id/certitude/revele_comment + 4 revele_si gates), caractere (curseurs 6 keys total-when-present, parler[], jamais, cede_si)
    dossier-objets/     # n° 5 — 2/2 TERMINEE: PanneauObjets + FicheObjet, registre d'objets (nom + description_joueur, sans borne), reorder via boutons Monter/Descendre composes par la feature, + retrait (RetirerObjetDialog, refus SSOT sans lot contrat)
    dossier-registres/  # n° 6 — 5/5 TERMINEE: indices, jalons & fins, quetes (EditeurEffets, premier editeur de Delta[]), evenements (filtre lie_a_histoire, monstre du bestiaire, jeton de remontage), et PanneauConditions + FicheClimat (nom, duree entiere en pas d'horloge, manifestation lue par le narrateur ; AUCUN EditeurEffets sur un climat)
    dossier-controles/  # n° 7 — 1/4 EN COURS: le linter d'aventure. controles.ts (registre ferme, rapport derive, seuil jouable) + PanneauControles/ListeControles + un SECOND landmark de nav. it1 = la regle « amorce non redigee »
    book-creation/      # name a book, seed the starting document, open the editor
    book-library/       # home screen: list / download / delete adventure dossiers
    tree-canvas/        # § 02 graph view — code untouched, dormant since n°2; visual repointing UNASSIGNED (KR-204, out of scope for n°6)
    cloud-sync/         # local-first sync decorator + conflict resolution
    play-mode/          # modal shell over the player runtime
  player/           # extractable play runtime — imports only pure brain/ functions
```

**Principles** (see `CLAUDE.md` for the full set):

- **Single source of truth**: the book lives in `BookService`. Canvas and preview are views — never private copies (KR-020).
- **Feature isolation**: a feature never imports another feature; cross-feature communication goes through `brain/` contracts only.
- **Persistence only via `PersistenceService` / `persistenceKeys.ts`** — no raw `localStorage` in feature code.
- **Derived state computed inline** (no `useEffect` mirroring); **references by stable id**, never by name.
- **Design fidelity**: render from `styles/` tokens + `brain/components/` primitives only; light theme; ≥44px hit targets; keyboard-operable.

## Domain model — en bascule

> **Le produit bascule** de « un livre = un arbre de nœuds + arêtes » vers « un **dossier d'aventure** joué par une IA ». Le plan exécutable est [`docs/ROADMAP-BASCULE-IA.md`](./docs/ROADMAP-BASCULE-IA.md) — décisions tranchées, ordre des features, ce qui a été supprimé et pourquoi. Le modèle décrit ci-dessous est celui qu'implémente le code **aujourd'hui** ; la feature n° 1 `dossier-format` le remplace.

Node kinds: `sommaire` (root) · `choix` · `pnj` · `decor` · `piege` · `monstre` · `fin` · `mort` (locked death leaf). Edges carry a `kind`: `choice` | `relink` | `flee`. Every new book is seeded with **exactly two nodes**: a `sommaire` (empty text zone, root) and an **isolated, locked** `mort` node (0 edges) — never auto-linked on create.

## Features

Huit features ont été supprimées le 2026-08-03 par la décision D3 (`outline-view`, `node-editor`, `choice-linking`, `book-export`, et les quatre `action-*`) : elles étaient câblées sur des types de nœuds, des choix et des formats d'export que la bascule abandonne. Leur code est dans l'historique git.

**Deux features neuves se sont achevées** — la n° 1, le contrat entre les deux temps, et la n° 2, qui bascule l'éditeur lui-même :

| Feature | État | Résumé |
| --- | --- | --- |
| `dossier-format` | **5/5 ✅ terminée** | Le **dossier d'aventure** : `schema: 1` en trois racines (`canon` / `monde` / `charpente`), validateur qui refuse **en français rédigé** (19 codes, l'entité nommée, pas un chemin JSON), gel en profondeur dès l'import, round-trip import/export, et l'affordance « ⬚ Importer un dossier » dans la bibliothèque. Les **conditions** sont un arbre `ExprNode` sans aucun parseur, piloté par le registre fermé `PREDICATES` (7 entrées) : une condition qui référence une entité inexistante est refusée par son nom. Les **effets** sont un registre fermé `DELTAS` (4 entrées, slots de référence seuls) : un effet qui pointe une entité inexistante, une opération que le moteur ne reconnaît pas ou un élément de liste qui n'est pas un objet sont refusés par leur nom. it5 livre le **dossier de référence** (`dossier-reference.json`, 6 PNJ / 5 lieux, distinct de la fixture minimale) et sa checklist de suffisance à sept branches nommées. *(La forme complète des treize racines n'appartient plus à cette feature — décision A : chaque racine la reçoit dans la feature qui l'édite, n° 3 à n° 6.)* |
| `bascule-editeur` | **3/3 ✅ terminée** | La navigation latérale passe de l'arbre à une liste de sections. it1 repointe `book-library` sur `DossierService` (`list`/`remove`, `DossierResume` en union discriminée lisible/illisible) : la bibliothèque liste, télécharge et supprime des dossiers d'aventure, BUG-048 (dossier-format) fermé pour de bon par un constat de présence par clé. `tree-canvas` mis en sommeil (repointage réel reporté à n° 6, faute de données relationnelles avant n° 4/5/6). it2 repointe `book-creation` sur `Dossier` (`DossierService.create`, seed marqué `⟨à écrire⟩` jamais joué tel quel), fait naître `DossierEditorScreen` (écran minimal, nouveau fichier — `EditorScreen.tsx` reste intact) sur une nouvelle route `{name:'dossier'}`, et rend une carte de bibliothèque cliquable pour rouvrir un dossier déjà présent. it3 livre la vraie nav : `SECTIONS` (registre `brain/`, dix sections comptées, jamais recalculées localement) rendue par `ListRow` (`brain/components`, portée sans sa poignée de glisser) sur deux colonnes, panneau droit à état vide nommé par section, `useOpenDossier` pour le rafraîchissement en direct. `EditorScreen.tsx`/`App.tsx`/le type `Route` restent intouchés — la démolition physique reste n° 9, et le critère de sortie est désormais un verrou anti-régression (aucun code de production ne navigue plus vers l'écran Book) plutôt qu'un grep de contenu. |

**Trois features neuves de plus se sont achevées** — n° 3 (le canon et ses annexes), n° 4 (la fiche personnage) et n° 5 (le registre des objets) :

| Feature | État | Résumé |
| --- | --- | --- |
| `dossier-canon` | **4/4 — terminée** — Canon ✅ · Départ ✅ · Objectifs ✅ · Lieux ✅ | it1 pose `DossierService.update(id, recette)`, le premier chemin d'écriture réel du dossier (re-validation, refus sans écriture, avertissements non bloquants, `dossier:updated`), et le premier vrai panneau de l'écran d'édition : synopsis MJ, accroche joueur, ton et interdits de ton, avec brouillon local (aucun revert sur refus) et compteur de mots. `IssueList` et `compterMots` sont promus dans `brain/`, injectés sur `DossierEditorScreen` via un slot (`panneaux`) sans aucun import croisé avec `bascule-editeur`. it2 ajoute Départ : un `Select` (`brain/components/Select.tsx`, jusqu'ici générique sans appelant) choisit `charpente.depart.lieu_id` parmi les lieux existants et commit immédiatement — sans brouillon, une référence n'a pas de mi-chemin éditable — libellé par `localiserEntite()` désormais exportée du baril ; le texte d'ouverture garde le patron brouillon+blur. it3 ajoute Objectifs : `Objectif.camp` (registre fermé `CAMPS`, `frapperIdentifiant()` promu à `brain/dossier/identifiers.ts`), un composant séparé `ObjectifsCanon` (liste libre, camp commité sans brouillon, nom/conditions en brouillon+blur), et l'avertissement D1 `condition-sans-expr` rendu par une lecture dérivée de `validateDossier` — visible dès le montage, pas seulement après une saisie. it4 ajoute Lieux et **termine la feature** : `Lieu extends Entite` (`description`/`ambiance`/`dangers`, destination `'ia'`), layout liste-à-gauche/fiche-à-droite (`PanneauLieux` + `FicheLieu`), retrait immédiat sans `Modal` — le seul cas dangereux est déjà bloqué par le validateur. |
| `dossier-fiches` | **8/8 — terminée** — Camp, plan & rattachement ✅ · Identité ✅ · Caractéristiques ✅ · Objectif & plan d'actions ✅ · Relations & Présence ✅ · Savoirs ✅ · Retrait d'un personnage ✅ · Caractère exploitable ✅ | L'écran le plus important du produit : une fiche à accordéon par personnage (`PanneauPersonnages` + `FichePersonnage`), remplie en **8 itérations verticales**, chacune une **tranche de schéma** que son lot contrat pouvait porter seule (KR-190) — jamais deux tranches dans la même itération. it1 pose `camp`/`portee`/`objectif_id` (camp **optionnel**, KR-191 : `schema: 1` sans migration) et l'`Accordion` (composant local, KR-109). it2 ajoute l'identité (`fonction`/`apparence`/`description_joueur`, `ia`) et durcit l'écriture : `RefusEnCours` porte ses **deux** indexations affichage/invalidation (KR-197). it3 ajoute les 8 caractéristiques et le PV dérivé : `stats` est **optionnel en bloc, TOTAL quand présent** — jamais `Partial` — un geste explicite sème les 8 clés, jamais un repli de lecture (BUG-064) ; le PV (`maxPV`) n'est **jamais stocké** (KR-192). it4 ajoute `but` et le plan d'actions (`duree`/`si_bloque`), et pour un antagoniste des **contre-mesures** en section interne, jamais un 9ᵉ bloc (KR-196). it5 situe le personnage dans le monde — relations (intensité signée, `secret` conditionné au **rôle** qui demande) et présence — après un redécoupage en deux temps qui fait passer la feature de 6 à **8** itérations (retrait → it7, savoirs → it6). it6 ajoute les savoirs et leurs 4 portes de révélation indépendantes, sans coût de contrat (le type existait déjà depuis `dossier-format`). it7 livre le retrait d'un personnage : modale de confirmation, refus **au SSOT** si une autre entité le référence encore, **aucun pré-vol côté feature** (l'auto-référence, KR-194, doit rester légale). **it8 clôt la feature** : le caractère exploitable — 6 curseurs (méfiance, franchise, courage, cupidité, loyauté, verve, mappés CA/IN/IG, donnée de **présentation**, KR-193, hors mutation et hors table dorée), jusqu'à deux répliques types, une limite absolue et une condition de reddition (`cede_si`, injectée au seul rôle acteur du **porteur** — patron `Relation.secret`, SECOND champ du schéma dont l'injection dépend du rôle qui demande). Deux classes de défaut nées ailleurs (KR-197 ; KR-199, un test vert au nom plus large que ses assertions) ont chacune resurgi plusieurs fois avant promotion en discipline de raffinage ; la dette KR-112 (hooks d'écriture, `FichePersonnage.tsx`) s'est redéplacée d'itération en itération sans jamais totalement se résorber, clôturant à 418 lignes — sous le bloqueur (800). Détail itération par itération : `src/features/dossier-fiches/specification.json`, `.claude/raffinage/dossier-fiches-it{1..8}.revue.md`. |
| `dossier-objets` | **2/2 — terminée** — registre ✅ · retrait ✅ | it1 pose `Objet extends Entite { description_joueur?: string }` (`ia`, sans borne — même famille que `Lieu.description`/`Personnage.description_joueur`) et remplace l'état vide de la section Objets par `PanneauObjets` + `FicheObjet` (liste-à-gauche/fiche-à-droite, précédent `PanneauLieux`/`FicheLieu`). Le réordonnancement, présupposé au cadrage comme une poignée de glisser câblée sur `ListRow.onReorder`, a été écarté au raffinage par les 4 rôles (aucun équivalent clavier, non prouvable en jsdom, anti-patron KR-109) : deux boutons Monter/Descendre, composés **entièrement par la feature**, `ListRow.tsx` restant inchangé en code (docstring seul). **it2 clôt la feature** : retrait d'un objet confirmé par une modale (`RetirerObjetDialog`, précédent `RetirerPersonnageDialog.tsx`), refusé et motivé (personnage porteur nommé) au SSOT sans qu'aucun fichier `brain/` n'ait été nécessaire — le refus par référence pendante était déjà couvert depuis dossier-fiches it6. |

**La septième feature est commencée** — `dossier-controles`, le linter d'aventure : un écran « Contrôles » qui dit à l'auteur ce qui empêche encore son aventure d'être jouable. Son itération 1 pose le contrat (`brain/dossier/controles.ts` — registre fermé de règles, rapport dérivé, seuil `jouable`) et la première règle : les quatre proses semées à la création et jamais réécrites, **bloquante** sur le texte d'ouverture (que le moteur lirait au joueur mot pour mot), **alerte** sur les trois proses du canon (qui partiraient telles quelles dans le contexte du modèle). Le cadrage a corrigé le plan-cible sur quatre points mesurés : les deux règles que D1 lui attribuait sont **déjà livrées** dans `validateDossier` — et l'une d'elles est structurellement **inobservable** dans l'éditeur, `DossierService.update` refusant toute écriture porteuse d'une `error` (KR-225) ; « Intrigue en second plan » et « Difficulté non calibrée » **sortent** du périmètre ; « PNJ inatteignable » est **amputée**, faute de graphe de lieux. Décompte final : **10 règles** (5 bloquantes, 4 alertes, 1 info), et non 8.

**La sixième feature est terminée** — `dossier-registres`, le registre des quatre dernières racines du monde (indices, quêtes, événements, climat) plus l'écran des jalons & fins. Avec elle, **les dix sections du dossier ont un vrai formulaire** et la dernière racine du schéma est refermée :

| Feature | État | Résumé |
| --- | --- | --- |
| `dossier-registres` | **5/5 — terminée** — Indices ✅ · Jalons & fins ✅ · Quêtes ✅ · Événements ✅ · Climat ✅ | **it5 clôt Climat et la feature** : `Climat` gagne `duree?: number` (ENTIER, `CHAMPS_ENTIERS`, `min DUREE_MIN`, destination `moteur` — la collision de nom avec `plan_actions[].duree` s'éteint au lieu d'être contournée, KR-198) et `manifestation?: string` (audience `ia`, bornée à 20 mots — la phrase que le narrateur lit tant que le climat est actif) ; `PanneauConditions` + `FicheClimat` remplacent le dernier état vide de l'éditeur. **Aucun `EditeurEffets` sur un climat** — et le motif écrit au cadrage pour ce refus était FAUX : `KR-208` n'interdit qu'une opération à opérande entier _neuve_, pas l'usage des quatre existantes ; l'UX l'a démontré seule contre quatre, et la conclusion tient sur deux autres motifs (un climat n'a **aucun instant d'application** et **aucune idempotence**, le besoin étant déjà servi par un Événement). `manifestation` est l'unique proposition `INNOVATION` de la feature : sans elle, `monde.conditions.climat[]` serait la seule collection de registre du schéma à zéro champ `ia`. Premier raffinage de la feature à **5 rôles** (`narratif-ia` convoqué pour une question d'audience). 2 lots séquentiels, 81 suites / 1173 tests verts. Reste non tenu et écrit comme tel : le § 09 du plan-cible (« effets chiffrés sur les règles »), faute d'opération `Delta` à opérande entier. — it1 pose `Indice extends Entite { portee?, verite?, formulation_joueur?, mene_a?: string[] }` — `monde.indices` cesse d'être un `Entite[]` nu — et remplace l'état vide de la section Indices par `PanneauIndices` + `FicheIndice`. `mene_a[]` (le chaînage vers d'autres indices) se rend en **liste textuelle d'identifiants** : le graphe visuel (repointage `tree-canvas`) est **hors périmètre total** de cette feature (KR-204, veto tech-lead unanime). `portee` reste posé au type sans champ de formulaire — aucun consommateur réel cette itération, même anti-patron que `tier`/`lie_au_canon` déjà écartés ; revient en bloc (type+écran) avec son consommateur (candidat n° 7). Trois trous de validation latents fermés au passage (`mene_a[]` est la première liste de références du schéma) : valeur non-string silencieusement ignorée, message d'erreur mal formaté, valeur non-tableau plantant le panneau — voir KR-212/213. `avecOrpheline()` promue de `dossier-fiches/BlocSavoirs.tsx` vers `brain/utils/references.ts` (KR-110). L'auto-référence dans `mene_a` est exclue à l'ajout mais tolérée/résolue si déjà persistée (précédent `apres_indice_id`). 2 lots séquentiels, 77 suites / 1113 tests verts. **it2 clôt Jalons & fins** : `Fin.texte?: string` (seul ajout de schéma, destination `moteur`, jamais gatant) et `PanneauJalonsFins` + `FicheJalon`/`FicheFin` remplacent l'état vide. `Jalon.enonce_texte`/`declencheur_texte` et `Fin.condition_texte` sont les premiers champs `CHAMPS_REQUIS` qu'un écran de cette feature doit créer : le geste d'ajout d'it1 (entité quasi nue) y est refusé par le validateur — résolu par un **brouillon différé** (l'entité reste hors du document tant que ses champs requis ne sont pas tous non vides), précédent réel `useEcriturePlan.ts` (`dossier-fiches`), après qu'un semis par texte-marqueur a été écarté au raffinage (il aurait allumé l'avertissement D1 sur toute fin neuve) — voir KR-214. L'avertissement D1 est rendu uniquement sur la fiche Fin (`alerteSansExpr:true`), jamais sur Jalon (`alerteSansExpr:false`, silencieux par design). 2 lots séquentiels, zéro fichier `brain/` pour le lot écran, 78 suites / 1127 tests verts. **it3 clôt Quêtes** : `Quete extends Entite` gagne `donneur_id?`/`consigne?`/`etapes?: EtapeQuete[]`/`echeance?` (`recompense: Delta[]` inchangé depuis `dossier-format`), et `PanneauQuetes` + `FicheQuete` + `EditeurEffets` remplacent l'état vide. `EditeurEffets` est le **premier éditeur de `Delta[]`** du dépôt — générique sur `DELTAS[].refKinds.length`, ajout à deux temps, réutilisé sans fork par l'itération Événements. `DELTAS` n'était exporté d'aucun fichier hors `brain/dossier/` (même famille de trou que `MARQUEUR_A_ECRIRE` en it2) : résolu par export nommé + allow-list dans `deltas.test.ts` — voir KR-215. `EtapeQuete` ne porte aucun champ `etape` : l'ordre est celui du tableau, jamais un ordinal stocké (KR-013). 2 lots séquentiels, zéro fichier `brain/` pour le lot écran, 79 suites / 1145 tests verts. **it4 clôt Événements** : `Evenement` gagne `lie_a_histoire?: boolean` (`ENUMERES_FERMES`, absent = libre en lecture, toujours écrit explicitement à la création), et `PanneauEvenements` + `FicheEvenement` remplacent l'état vide — `SegmentedControl` en **filtre** d'une seule collection (pas une bascule entre deux collections, contrairement à it2), un monstre du bestiaire optionnel (`Select` vers `BESTIARY`, sans `avecOrpheline` — registre constant), et des résolutions (`resolutions[]`, déjà typées depuis `dossier-format`) dont les conséquences s'éditent via `EditeurEffets` réutilisé **sans une ligne modifiée**. `Evenement.nature` REJETÉ au complet au raffinage (aucun consommateur pour `scene`/`obstacle`, `monstre` dérivé de `monstre_ref` — 3ᵉ occurrence de l'anti-patron `tier`/`lie_au_canon`/`Indice.portee`, KR-206). Le retrait d'une résolution, remis dans le périmètre au tour 2 du raffinage, est protégé par un « jeton de remontage » (compteur monotone dans la clé React, KR-216) qui évite qu'un brouillon d'effet migre vers la résolution voisine. 2 lots séquentiels, aucun ricochet sur les fixtures partagées. 80 suites / 1156 tests verts. |

Et **cinq autres survivent** à la bascule, toutes destinées à être repointées :

| Feature | État | Repointée en | Résumé |
| --- | --- | --- | --- |
| `tree-canvas` | iter 4 ✅ — **code intact, en sommeil depuis n° 2** | **non assignée** | § 02 vue graphe : cartes de nœuds + arêtes SVG sur trame de points ; liaison vive au `BookService` ; sélection unique ; pan/zoom + repositionnement persistés par livre (`UIPreferencesService`) ; culling hors écran. `EditorScreen.tsx` (racine de composition, hors de tout lot) monte toujours `TreeCanvas` sans modification ; depuis `bascule-editeur` it1, plus aucun chemin d'IHM n'y mène (book-creation/book-library ne naviguent plus vers la route éditeur). **`EditorScreen.tsx` reste intouché à l'issue d'it3** — le raffinage a mesuré que le vider ou le supprimer casserait dix tests d'intégration de `tree-canvas` lui-même (`TreeCanvas.test.tsx` monte `<App/>` sur la route `editor` et asserte le rendu de l'écran) ; sa démolition physique, comme celle de `BookService.ts`/`kinds.ts`/`tree.ts`, reste entièrement reportée à n° 9. **CORRIGÉ (cadrage `dossier-registres`, 2026-08-17)** : le repointage visuel n'est PLUS assigné à n° 6 — `tree-canvas` est câblé sur `BookNode`/`Edge` condamnés (une réécriture, pas un repointage), aucun instrument de test ne couvre pan/zoom/layout, et `lieux[].acces` n'a toujours pas de propriétaire (KR-200/205). `dossier-registres` livre seulement la **donnée** du graphe (`indices[].mene_a`, liste textuelle). Repointage visuel : chantier à part, non planifié. |
| `book-library` | **repointée — `bascule-editeur` it1+it2 ✅** | n° 2 | Écran d'accueil : grille de cartes de **dossiers d'aventure** (vue vive via `useDossiers`), télécharger / supprimer, recherche + tri, avis nommé si des livres pré-bascule restent invisibles. Depuis it2, le titre d'une carte lisible est cliquable et rouvre le dossier sur `DossierEditorScreen`. Renommer/dupliquer un dossier : hors périmètre, reportés. |
| `cloud-sync` | iter 5 ✅ | n° 1 | `CloudSyncService` local-first décorant `PersistenceService` ; poussées groupées et débattues ; file hors-ligne persistée ; résolution de conflit. Agnostique du document — seule la forme persistée change. |
| `book-creation` | iter 3 ✅ | n° 2 | « + Nouveau dossier » → `DossierService.create()` sème un dossier valide (canon + un lieu + un point de départ, tous les textes de prose marqués `⟨à écrire⟩`) → écran d'édition, en écriture local-first à travers le décorateur de synchronisation. **Repointée sur `Dossier` depuis `bascule-editeur` it2 ✅** — remonte dans la bibliothèque, dont elle était retirée depuis it1. |
| `play-mode` | squelette ✅ | n° 9 | Coquille modale au-dessus de `src/player/` (le runtime joueur extractible, ~4 400 lignes : combat, création de personnage, boutique d'XP). Suit le runtime. |

**Version `0.6.x` = Temps 1** (l'éditeur produit un dossier, features n° 1 à 8) ; `0.7.x` = **Temps 2** (le moteur joue le dossier, n° 9 à 16). PATCH +1 par itération livrée, dans l'ordre de la roadmap. Le modèle en paliers horizontaux (`0.1.x` MVP → `0.5.x` V4) est retiré : il a porté l'éditeur d'arbre jusqu'à `0.5.34`, l'historique est dans [`CHANGELOG.md`](./CHANGELOG.md).

> L'écran d'édition **de livre** (`EditorScreen.tsx`, route `editor`) est une **coquille intacte et injoignable** : barre supérieure + canevas d'arbre, sans panneau d'édition, plus aucun chemin d'IHM n'y mène depuis `bascule-editeur` it1 — sa démolition physique est reportée à n° 9. L'écran d'édition **de dossier** (`DossierEditorScreen`, route `dossier`, fichier neuf de `bascule-editeur`) affiche depuis it3 sa vraie navigation à deux colonnes : dix `ListRow` (Canon, Départ, Personnages, Lieux, Objets, Indices, Quêtes, Événements, Conditions, Jalons & fins) avec compteur exact à gauche. **Les dix** sections ont désormais un vrai formulaire, et il ne reste aucun état vide : Canon (`PanneauCanon` + `ObjectifsCanon`), Départ (`PanneauDepart`) et Lieux (`PanneauLieux` + `FicheLieu`) depuis `dossier-canon` it1→it4, Personnages (`PanneauPersonnages` + `FichePersonnage`, terminée 8/8) depuis `dossier-fiches`, Objets (`PanneauObjets` + `FicheObjet`, terminée 2/2) depuis `dossier-objets` it1→it2, et Indices (`PanneauIndices` + `FicheIndice`), Jalons & fins (`PanneauJalonsFins` + `FicheJalon`/`FicheFin`), Quêtes (`PanneauQuetes` + `FicheQuete` + `EditeurEffets`), Événements (`PanneauEvenements` + `FicheEvenement`) et Conditions (`PanneauConditions` + `FicheClimat`) depuis `dossier-registres` it1/5 → it5/5, terminée — tous injectés via le slot `panneaux` de `App.tsx`, jamais un import direct entre features.
