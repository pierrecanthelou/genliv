# genliv — Éditeur de « livre dont vous êtes le héros »

Authoring tool for gamebooks (choose-your-own-adventure). A **book = a tree of nodes + edges**, and that tree is the thing that persists. Scope is **editor/authoring mode only**; play mode is deferred. UI and domain terms are in **French**.

Design handoff and binding specs live in [`design_handoff_gamebook_editor/`](./design_handoff_gamebook_editor); always-on rules are in [`CLAUDE.md`](./CLAUDE.md).

## Stack

- **React 18 + TypeScript**, built with **Vite**.
- **Jest + React Testing Library** for unit/component tests (Playwright E2E planned).
- **ESLint + Prettier** (tabs, single quotes, no semicolons).

## Commands

```bash
npm install      # install dependencies
npm run dev      # Vite dev server (http://localhost:5173)
npm run build    # type-check + production build
npm test         # Jest unit/component tests
npm run lint     # ESLint
npm run typecheck# tsc --noEmit
```

## Architecture

```
src/
  main.tsx          # React root: wires brain + features
  App.tsx           # shell: maps route -> view
  style.css         # reset + design-system tokens (styles/)
  styles/           # design system: styles.css + tokens/*.css (colors, type, spacing, fonts)
  brain/            # core engine — feature-agnostic, never imports features/
    types.ts            # domain model (Book, BookNode, Edge, NodeKind, EdgeKind)
    EventBus.ts         # Observer: typed cross-feature events
    Router.ts           # navigation contract
    PersistenceService.ts + persistenceKeys.ts   # the only storage gateway (KR-011/111)
    BookService.ts      # single source of truth for the book tree; seed factory
    BrainContext.tsx    # DI via React Context (Service Locator)
    components/         # cross-feature presentational primitives (KR-109)
    utils/              # cross-feature utilities (KR-110), e.g. stable id generation
  features/         # isolated feature modules; talk to the rest only through brain/
    book-creation/      # name a book, seed Sommaire + isolated Mort, open the editor
    book-library/       # home screen: list / open / delete books
```

**Principles** (see `CLAUDE.md` for the full set):

- **Single source of truth**: the book (nodes + edges) lives in `BookService`. Canvas / outline / preview are views — never private copies (KR-020).
- **Feature isolation**: a feature never imports another feature; cross-feature communication goes through `brain/` contracts only.
- **Persistence only via `PersistenceService` / `persistenceKeys.ts`** — no raw `localStorage` in feature code.
- **Derived state computed inline** (no `useEffect` mirroring); **references by stable id**, never by name.
- **Design fidelity**: render from `styles/` tokens + `brain/components/` primitives only; light theme; ≥44px hit targets; keyboard-operable.

## Domain model

Node kinds: `sommaire` (root) · `choix` · `pnj` · `decor` · `piege` · `monstre` · `fin` · `mort` (locked death leaf). Edges carry a `kind`: `choice` | `relink` | `flee`. Every new book is seeded with **exactly two nodes**: a `sommaire` (empty text zone, root) and an **isolated, locked** `mort` node (0 edges) — never auto-linked on create.

## Features

| Feature          | Status    | Summary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `book-creation`  | iter 3 ✅ | « Nouveau livre » dialog → seed Sommaire + isolated Mort → editor, via `CreateBookEntry` (composed into the library home). **Iter 3 — cloud-first create**: a new book writes through the `CloudSyncService` decorator, so it is **local-first** (synchronous local write) + **queued for the background push** + **restored on reload** (already transparent via KR-093). The dialog now shows a **sync-aware cloud-first hint** (brain `useSyncStatus`, copy from a `Record<SyncStatus,string>`, KR-013/117): « Créer » works offline and the book is saved on-device first. **Feature complete (n=3).**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `book-library`   | iter 3 ✅ | Home screen `LibraryScreen`: grid of book cards (live VIEW via brain `useBooks`); click → open + navigate. Cards show écrans · liens · fins counts + « Modifié le {date} ». Hover/focus reveals **rename** / **duplicate** / delete. `BookService` gained `renameBook` + `duplicateBook` (deep copy, fresh ids remapped by stable id). Delete gated by a confirmation dialog. **Iter 2**: **search** + **sort** (Récent/A→Z) derived inline over the live list (KR-013); empty-state vs no-match; a composition-root **guard** that navigates home if the open book is deleted (KR-071). **Iter 3 — per-book cloud-sync status**: each card shows a chip — **« ✓ à jour »** / **« ⏳ en attente »** / **« ⚠ non synchronisé »** (none in a local-only build) — a live VIEW over `CloudSyncService` via the new generic `pendingKeys()` + brain `useBookPending(bookId)` hook (the decorator stays book-agnostic, KR-094/095). List reconciliation + offline-delete propagation deferred (need new `CloudTransport` capabilities).                                                                                                   |
| `tree-canvas`    | iter 3 ✅ | § 02 graph view: node cards (NodeBadge + ref + title + snippet) + SVG edges on a dot-grid; live binding to `BookService` via `node:*`/`edge:*`; single-select; « + Nœud » adds a free-floating node. **Iter 3 — view-state persistence**: a new brain **`UIPreferencesService`** (per-device, **never cloud-synced**, KR-022/025; raw local store) persists **pan/zoom**, the **canvas↔outline view-mode**, and **dragged node positions** per book. Nodes are now **drag-to-reposition** (`useNodeDrag`): a dragged position is a UI preference (not synced `node.position`) that **overrides** the auto-layout slot. Pan/zoom persists on settle (not per frame). Retires `EditorStub`. Iter 4 (robustness at scale) remains.                                                                                                                                                                                                                                                                                                                                                                                                     |
| `node-editor`    | iter 3 ✅ | § 02 side panel mounted beside the canvas: header (NodeBadge + ref + title + ✕), editable Description, Fin victoire/échec toggles, « Action requise » SegmentedControl mounting editors from `ActionRegistry`. Selection promoted to a brain `SelectionService` (SSOT). **Iter 2 (action seam)** was banked across the skeleton + all four `action-*` features registering against the registry (zero node-editor changes, KR-051); **iter 3 (« objets à prendre » inventory) superseded** by action-decor « Prendre » takeables (no separate node inventory; shared `ObjectCatalogService` deferred, KR-062). Remaining: iter 4 (debounced commits, a V4 item). Libellé/choices come from choice-linking via `SlotRegistry`; illustration is a deferred dropzone.                                                                                                                                                                                                                                                                                                                                                                  |
| `choice-linking` | iter 3 ✅ | Outgoing-choice rows in node-editor's choices slot (via brain `SlotRegistry`): « + branche » creates a child + `choice` edge and selects it; « Relier… » adds a `relink` edge to an existing node (cycles/convergence); remove deletes only the edge. Each row carries an editable **« libellé du choix »** persisted on `Edge.label`. **Iter 2**: `addEdge` guards **self-links + duplicate edges** at the SSOT (identity = `from`+`to`+`kind`, KR-061); searchable « Relier… » popover. **Iter 3 — hidden prerequisite (§ 05, KR-062)**: a per-row **« pré-requis caché »** toggle + object picker; the rule rides `Edge.prereq?: { objectId }` (`BookService.updateEdge`, `prereq: ChoicePrereq \| null`). The object catalog is a **derived VIEW** — brain `collectObjects(book)` unions décor takeables + PNJ gift + monster loot by stable id (no owned store, KR-020); a resolved id shows **⊘**, a dangling/unconfigured one shows **⚠** (KR-062/021). Countdown (⏱) stays for iter 4 (V4).                                                                                                                                 |
| `outline-view`   | iter 3 ✅ | The open book as an indented « plan » (`OutlineView`): DFS from the sommaire nesting `choice` edges, `relink`/`flee`/cycles shown as `↪` references (cycle-safe), isolated nodes flat. **Expand/collapse** (`computeVisibleRows`), **hover preview**, refined reference-row affordance. Shared editor `EditorTopBar` (brain) with a canvas ↔ outline switch; selection shared via `SelectionService`. **Iter 2** adds the **node inspector** (§ 03 B): `buildNodeInspector` (« entre depuis » + monstre victoire/fuite, dangling refs surfaced KR-021) with **Éditer** + **« Centrer dans l'arbre »** (reveal + centre via a `{nodeId, seq}` `RevealRequest` + `useViewport.centerOn`, KR-081). **Iter 3 — view-state persistence**: the canvas↔outline **view-mode** (already persisted via `useBookViewMode` since tree-canvas iter 3) plus the **outline expand/collapse state** now persist per book via `UIPreferencesService` (`outlineCollapsed` + new brain `useBookOutlineCollapsed` hook, non-synced KR-022/025) — collapsing survives a view switch + reload. Rule badges (⊘/⏱) await choice-linking iter 4 (countdown). |
| `action-decor`   | iter 3 ✅ | First `action-*` feature: self-registers a « Décor » editor with the brain `ActionRegistry`. Prendre / Écouter / Fouiller switch on `node.decor`. **« Prendre »**: takeable objects as rows (utile/leurre `Record` KR-117, « jet » marker), each edited in a **modal** with the shared **`ObjectEditor`** (KR-052) + optional « jet requis ». **Iter 2 — Écouter/Fouiller**: a `RevealEditor` (heard/found text + skill-roll gate via the shared `OutcomesEditor`, KR-091) — per-interaction reveal (BUG-007). **Iter 3 — reuse « prendre dans la liste »**: a `TakeableObject` is now `own { object }` **or** `ref { objectRef }` reusing an existing catalog object **by stable id** (resolved live via `collectObjects`/`findObject`, **no owned store**, KR-062/020). A searchable `ReuseObjectPicker` adds a reference; a ref row is read-only (**« réutilisé »**) and a dangling ref shows **« ⚠ objet supprimé »** (KR-021).                                                                                                                                                                                                 |
| `action-pnj`     | iter 3 ✅ | Second `action-*`: self-registers a « PNJ » editor. NAME + DIALOGUE on `node.pnj`; a « donne un objet » gift (effect `Record` KR-117 + value stepper) via the shared `ObjectEditor`; **« mène à »** target picker. **Iter 2 — richer identity**: a **RÔLE** field + a deferred portrait dropzone. **Iter 3 — reusable PNJ by reference**: a PNJ node can **reuse an existing PNJ « du livre »** by stable id — the **owner node id is the PNJ's stable id** (no new id, KR-096); `PnjConfig.pnjRef` references it, resolved **live** via derived `collectPnjs`/`resolvePnj`, so the PNJ stays single-sourced (KR-020). A searchable `PnjPicker` adds the reference; a referenced PNJ is read-only (**« réutilisé »** + detach), a dangling one shows **« ⚠ PNJ introuvable »** (KR-021).                                                                                                                                                                                                                                                                                                                                            |
| `action-monster` | iter 3 ✅ | Third `action-*`: « Monstre » editor — PV/Attaque/Défense `Stepper`s, réussite/échec `OutcomesEditor` (KR-091), victoire/fuite `TargetPicker`s, défaite→Mort automatic (KR-067), « butin lâché » via the shared `ObjectEditor`. **Iter 3 — reusable monster library**: « Ajouter à la librairie » persists to a new brain **`MonsterLibraryService`** (cross-book, raw local store, not synced) and a searchable **`MonsterLibraryPicker`** instantiates a saved monster onto a node as an independent **copy** (targets stripped, fresh loot id, KR-097); `monster:savedToLibrary` now consumed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `action-trap`    | iter 3 ✅ | Fourth `action-*`: « Piège » editor — description + réussite/échec via the shared `OutcomesEditor`, a « échec sanctionné » fatal flag. **Iter 2**: the fatal flag drives the automatic **→Mort** link as a **view-derived** edge (`deriveAutomaticEdges`, KR-067), never the manual edge API. **Iter 3 — trap-on-object**: a décor « Prendre » object's jet requis can be **« Variante piège : échec → mort »** (`SkillRoll.fatal`); `deriveAutomaticEdges` emits the same fatal→Mort edge for it. The trap capability rides décor's takeable editor — the two features share only the brain `SkillRoll` + `deriveAutomaticEdges`, never import each other.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `cloud-sync`     | iter 2 ✅ | Local-first **`CloudSyncService`** (brain) decorating `PersistenceService`. The **transport is chosen by build target** (local **`LocalStorageTransport`** fake remote / Cloudflare worker later). **Debounced + batched** pushes; **last-write-wins reconciliation on `book:opened`** (KR-094). **Iter 2 — offline write queue**: pending pushes are persisted (`CLOUDSYNC_QUEUE_KEY`) so an unconfirmed write **survives a reload** and **flushes on reconnect** (next write / `retry()` / startup); a failed push keeps the queue (never dropped). `SyncIndicator` surfaces **« N changements en attente »** via `useSyncPending` (KR-095). Unblocks `book-creation` iter 3. Conflict handling is iter 3. `SyncIndicator` corner pill via `useSyncStatus`.                                                                                                                                                                                                                                                                                                                                                                       |

**The `0.2.x` / V1 tier is complete** — every feature has its iteration 1. We build **breadth-first** (see `docs/ROADMAP.md`): `0.1.x` = MVP, `0.2.x` = V1, `0.3.x` = V2, etc. **🏁 Tier `0.3.x` / V2 is in progress** — iteration 2 of each feature, in build order: `choice-linking` iter 2 (relink popover + self-link/duplicate guards), `book-library` iter 2 (search/sort + empty state + open-book delete guard), `outline-view` iter 2 (node inspector + « Centrer dans l'arbre »), `action-decor` iter 2 (Écouter/Fouiller reveal + roll gate), `action-pnj` iter 2 (richer identity: rôle + deferred portrait), `action-monster` iter 2 (« butin lâché » loot), `action-trap` iter 2 (automatic →Mort link via derived edges), and `cloud-sync` iter 2 (offline write queue) shipped. **🏁 The `0.3.x` / V2 tier is complete** — every feature has its iteration 2 (`tree-canvas` iter-2 banked depth-first). **🚀 Tier `0.4.x` / V3 has opened** — iteration 3 of each feature with one, in build order: `book-creation` iter 3 (cloud-first create hint + contract tests), `tree-canvas` iter 3 (`UIPreferencesService` + pan/zoom/view-mode/drag persistence), `node-editor` iter 3 (superseded — inventory absorbed by action-decor « prendre »), `choice-linking` iter 3 (hidden prerequisite via a derived object catalog), `book-library` iter 3 (per-book cloud-sync status chip), `outline-view` iter 3 (persist outline collapse + view-mode via `UIPreferencesService`), `action-decor` iter 3 (reuse an object « prendre dans la liste » by id), `action-pnj` iter 3 (reuse a PNJ « du livre »), `action-monster` iter 3 (reusable monster library), and `action-trap` iter 3 (trap-on-object via the shared roll + derived fatal edge) shipped.

> The editor is now `tree-canvas` (graph) + `node-editor` (side panel), two isolated features composed by the app shell and talking only through brain (`SelectionService` + `BookService`). The four `action-*` features will self-register their required-action editors with `ActionRegistry`; `node-editor` mounts them without importing them.
