# Cadrage — `dossier-controles` (n° 7), itération 3

1. **`goal` brut, APRÈS coupe validée par l'humain le 2026-09-15** : livrer les **cinq règles de registre** du linter — `indice orphelin` (bloquant), `goulot d'étranglement` (alerte), `lieu de départ désert` (bloquant), `personnage sans présence` (alerte), `personnage sans voix propre` (info). Le pont vers les avertissements de `validateDossier` est **SORTI** vers une itération 4 NEUVE ; l'atteignabilité + « canon sans objectif » glissent en n° 5. Motif de la coupe : aucune phrase de démo ne couvrait les deux moitiés, et le compte de critères montait à 9–10.
2. **Phrase de démo visée** : « à la fin, l'auteur voit qu'un indice que personne ne détient rend son aventure injouable. »
3. **État de la feature** : it1 et it2 sont `done`. **Aucune surface neuve n'est attendue** : `ListeControles` rend déjà n'importe quel `Controle[]` (anatomie 3 lignes + pastille), `RapportControles.parSection` allume déjà les badges de `SectionNav` (it2), `pastilles.ts` est consommé **tel quel** — les cinq règles n'ajoutent **aucune forme**, seulement des entrées au registre `CONTROLES`.
4. **Fichiers probablement concernés** : `src/brain/dossier/controles.ts` (R), `src/brain/dossier/controles.test.ts` (R), une **fixture d'exercice locale** (N), et éventuellement `src/features/dossier-controles/tests/panneauControles.test.tsx` (R) pour la preuve verticale. À confirmer/infirmer par le tech-lead.
5. **KR applicables** : KR-217 (type FRÈRE de `DossierIssue`, jamais le canal `errors`/`warnings`) · KR-219 (la `section` est DÉCLARÉE par la règle, jamais dérivée du `path`) · KR-221 (« absent ≠ vide » sur `caractere` : jamais une VALEUR de curseur, seulement présence/absence de clé) · KR-224 (monde ouvert assumé — « personnage sans présence » reste AMPUTÉE de « lieu non relié au départ », faute de graphe) · KR-164 (un code par CAUSE, jamais par emplacement) · KR-199 (balayer depuis `CONTROLES`, jamais N littéraux) · KR-197/202 (DEUX entités dans le MÊME test : une qui déclenche, une qui ne déclenche pas) · KR-175 (`estCleDe`, jamais `in`).
6. **`resolved_decisions` qui s'appliquent** : preuve par **CLONE de `dossier-minimal.json` muté d'UN SEUL champ**, rouge puis calme dans le MÊME test — jamais une troisième fixture partagée, jamais une modification des deux fixtures existantes (lues par huit suites) · `niveau` et `section` vivent sur le **CONSTAT**, pas sur le descripteur · le `path` d'un contrôle, indices de collection normalisés en `[]`, est une **CLÉ de `DESTINATION_DES_CHAMPS`** · le `tone` se prouve **AU CONTRAT**, jamais au rendu (`toHaveStyle` est **cassé** sur les jetons `var()` — il passe sur n'importe quelle teinte) · **aucune règle bloquante ne prend pour critère un champ d'audience `ia`**.
7. **Matériau mesuré dans le schéma** : `Savoir.indice_id` · l'effet `reveler_indice` du registre `DELTAS` (`refKinds: ['indice']`) · `Indice.mene_a?: string[]` (auto-référence LÉGALE) · `Personnage.presence?: Presence[]` → `presence[].lieu_id` · `Personnage.caractere?: Caractere` → `caractere.parler?: string[]` (les quatre champs de `Caractere` sont indépendants et optionnels) · `charpente.depart.lieu_id`. Les quatre chemins `monde.indices[].id`, `monde.personnages[].presence[].lieu_id`, `monde.personnages[].caractere.parler[]`, `charpente.depart.lieu_id` **SONT** des clés de `DESTINATION_DES_CHAMPS` (vérifié).
8. **Tension mesurée n° 1 — le `path` d'une règle qui se déclenche sur une ABSENCE.** Trois des cinq règles tirent sur un champ qui **n'existe pas** (aucun savoir ne détient l'indice ; `presence` absente ou vide ; `parler` absent ou vide). Or `destinations.ts` ne porte **aucune** clé `monde.personnages[].presence` ni `monde.personnages[].caractere.parler` **sans** le suffixe `[]`. La règle d'it1 « le `path` est une clé de `DESTINATION_DES_CHAMPS` » n'a donc pas de cible évidente ici. À trancher.
9. **Tension mesurée n° 2 — l'audience de `savoirs[].indice_id`.** La `resolved_decision` d'it1 affirme : « vérification nominale faite sur les cinq règles bloquantes : elles ne lisent que du **moteur** — … `savoirs[].indice_id`, `presence[].lieu_id` … ». **MESURE** : `src/brain/dossier/destinations.ts` l. 261 range `'monde.personnages[].savoirs[].indice_id': 'ia'`. La vérification nominale d'it1 est donc **fausse sur ce chemin**, et « indice orphelin » est proposée **BLOQUANTE**. Soit la règle d'audience souffre une exception motivée, soit le niveau tombe, soit la règle lit un autre champ. À trancher.
10. **Tension n° 3 — un report différé DEUX fois.** Le **clic d'une ligne du panneau pour sélectionner sa section** a été reporté d'it1 vers it2, puis d'it2 vers it3 (`it2.revue.md` l. 97 : « pour it3 : elle rouvrira `SectionNav.tsx` et `DossierEditorScreen.tsx` »). Chiffré par le tech-lead à it1 : **zéro fichier supplémentaire, ~7 lignes, +2 tests**, via un render-prop `panneauControles?: (api) => ReactNode`. **L'y intégrer ferait franchir DEUX signaux de coupe** (une deuxième feature touchée, et le 9ᵉ critère) ; l'en sortir serait un **troisième** report. Position à prendre, nommément — c'est le terrain du PM.
11. **Hors périmètre acquis, non rouvrable** : le pont des avertissements de `validateDossier` (it4 neuve) · l'atteignabilité et « canon sans objectif » (it5) · `Indice.portee` et « Intrigue en second plan » (rejetés au cadrage) · « Difficulté non calibrée » (n° 16) · le focus **dans le champ fautif** d'une fiche (open question, traverse 4 features) · le rendu de `jouable` en verdict global (n° 9) · toute assertion de teinte au rendu.
