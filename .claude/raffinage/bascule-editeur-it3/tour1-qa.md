RISQUE — le risque majeur de cette itération vu de la testabilité (mesuré, pas supposé)

J'ai vérifié le dépôt : `src/App.tsx` a bien 0 occurrence des 3 chaînes cibles aujourd'hui (confirmé), mais `src/EditorScreen.tsx` (69 lignes) en porte les trois — `TreeCanvas` (import ligne 3 + JSX ligne 64), `books.` (déstructuration ligne 17 + `books.addNode` ligne 37), `BookService` (commentaire ligne 10). Le critère #30 tel qu'écrit suppose implicitement que ce fichier continue d'exister pour être grepé (« ne trouve aucune occurrence »). Or le point ouvert #7 du cadrage n'est pas tranché : SUPPRIMER le fichier rend l'assertion `toContain`/`not.toContain` inapplicable (ENOENT), VIDER le fichier la rend triviale — deux formes de test radicalement différentes, deux plans de lot différents. Second fait mesuré : `src/features/tree-canvas/tests/` contient **3 suites / 37 tests** (`TreeCanvas.test.tsx`, `geometry.test.ts`, `nodeView.test.ts`), tous verts à l'instant — pas « ~5 suites » comme énoncé dans le cadrage. Ces 3 suites, plus `src/features/cloud-sync/tests/ConflictDialog.test.tsx` (2 tests), naviguent directement vers `{ name: 'editor', bookId }` via `brain.router.navigate` sans jamais rendre `EditorScreen.tsx` ni `App.tsx` — elles dépendent seulement du type `Route` (`brain/Router.ts`) qui conserve la variante `'editor'`. Toucher ce type casserait un test hors du périmètre nommé par le KR-181.

OBJECTION

Le critère #30, tel qu'écrit dans `specification.json` (« un test-grep sur src/App.tsx et src/EditorScreen.tsx ne trouve aucune occurrence de TreeCanvas, books., BookService »), n'est pas observable de façon univoque tant que le point ouvert #7 n'est pas tranché : une assertion `fs.readFileSync` sur un fichier supprimé n'est pas « aucune occurrence trouvée », c'est une erreur de lecture — un instrument différent. Le plan d'itération ne peut pas hériter cette phrase telle quelle ; il doit forker le critère selon la décision (fichier absent testé par `fs.existsSync(...) === false`, ou fichier vidé testé par le même patron `toContain` déjà utilisé avec succès dans `dossierEditorScreen.test.tsx:60-66`).

PROPOSITION

1. Trancher maintenant (pas au tour 2) : VIDER `src/EditorScreen.tsx` de ses imports/usages TreeCanvas/books./BookService plutôt que le supprimer. Motif testabilité : le patron grep (`fs.readFileSync` + `toContain`) déjà éprouvé dans `dossierEditorScreen.test.tsx` s'applique sans réécriture, et ça évite de toucher `Router.ts`/le ternaire de `App.tsx` dont dépendent typiquement les 3 suites tree-canvas + `ConflictDialog.test.tsx`. Si le comité préfère SUPPRIMER, le critère #30 doit être réécrit en deux assertions distinctes et nommées : `expect(fs.existsSync(cheminEditorScreenTsx)).toBe(false)` + grep inchangé sur `App.tsx` seul — à écrire dans le plan, pas laissé à l'ouvrier.

2. Fixer le baseline de non-régression dans la revue à la valeur **mesurée maintenant** : 3 suites / 37 tests verts sur `src/features/tree-canvas/tests/` (liste nommée : `TreeCanvas.test.tsx`, `geometry.test.ts`, `nodeView.test.ts`), plus `git diff --stat` vide sur ces 3 chemins après l'itération — jamais « environ 5 ». Ajouter `ConflictDialog.test.tsx` (2 tests, `src/features/cloud-sync/tests/`) à la même liste surveillée : il partage la dépendance à la variante `'editor'` du type `Route` sans être nommé par aucun KR de cette feature — un oubli plausible sinon.

3. Pour KR-013 (« compteur jamais recalculé localement, lu depuis SECTIONS »), nommer explicitement deux niveaux dans le plan, pas un critère générique de rendu : (a) unitaire sur `brain/dossier/sections.ts` — `compte(dossier)` par section, valeurs exactes ; (b) heuristique de revue — grep sur le fichier du composant de nav confirmant l'absence de `.length`/`.filter`/`.reduce` en dehors de `sections.ts` (le rendu seul ne peut pas distinguer une délégation d'un recalcul qui produit accidentellement le même chiffre).

VERDICT : recevable sous réserve — le point ouvert #7 doit être tranché et le critère #30 reformulé en conséquence avant écriture des lots ; le chiffre « suites tree-canvas » du cadrage doit être corrigé à 3/37 (mesuré) dans le plan et la revue finale.
