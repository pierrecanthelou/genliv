## Tour 2 — PM

**Fait vérifié (grep, avant tout arbitrage)** : `src/brain/dossier/types.ts` — zéro `camp`, zéro `relations[]`. `jalons[].declencheur_expr` est optionnel (ligne 303), `evenements[].declencheur_texte` ET `declencheur_expr` sont optionnels (194/198), `depart.texte_ouverture_joueur: string` existe et est obligatoire (284). Le désaccord n°1 se résout : **1 seule branche réellement sans ancrage ni proxy** (relation secrète). « jalon non obligatoire » et « événement non lié à l'histoire » (QA) sont des champs existants mal nommés dans le critère 9, pas des trous — reformulation, pas retrait. « Scène figée » se reformule sur un champ déjà livré, sans toucher au REPORTÉ n°10.

**Réponse nommée — Tech Lead, désaccord n°3 (phrase de démo).** Je défends la phrase déjà écrite au goal it5, ligne 148 : « l'auteur peut partir d'un dossier de référence complet plutôt que d'une page blanche. » Le verbe est « partir de », pas « télécharger ». Cette phrase est VRAIE en variante A : le fichier de référence, s'il vit en JSON réel sur disque (pas un littéral inline — déjà l'exigence du critère 8), est consommable tel quel par l'affordance d'import **déjà livrée en it1**. Aucune nouvelle surface n'est requise pour que la phrase soit honnête ; it5 n'a pas besoin d'exercer le clic, seulement de prouver le fichier complet et valide. Variante A confirmée, comme recommandé par le Tech Lead lui-même.

**Désaccord n°2 — antagoniste.** Je ne suis pas le Tech Lead ici. L'ancrage technique existe (`personnage` référencé dans `echoue_si_expr` via `pnj_a_revele`) mais le seul prédicat disponible parle de RÉVÉLATION D'INDICE, pas d'opposition — aucun champ ni prédicat ne modélise un rapport de force. Requalifier ça en « antagoniste » apprendrait un faux patron à l'auteur qui lit le dossier de référence comme modèle, ce qui est le rôle même de cet artefact. Je tranche pour le report avec `relation secrète`, même destination n°4 (`camp`/opposition y a un propriétaire).

**Désaccord n°5 — « budget-injectable ».** Je le compte comme une innovation et je la refuse ce tour. Précédent direct dans la même feature : `BUDGET_CONTEXTE` a été proposé puis retiré par son propre auteur au tour 2 d'it2 (spec, open_questions ligne 395), motif « une table sans lecteur est la dette qu'on reprocherait à quelqu'un d'autre » — aucun assembleur n'existe avant n°9/n°10. Le narratif rouvre la même dette sous un autre nom. Reste en `open_questions`, non codé.

**Désaccord n°4 — téléchargement.** Confirmé : hors it5, nommé n°2. Le goal-feature au roadmap (« …importer un dossier d'aventure validé… ») ne porte que l'import ; `exportDossier` reste prouvé par contrat (critère 8, déjà testé). Ferme l'open question ligne 396 explicitement plutôt que de la laisser traîner.

**Statut de mes items du tour 1**
- OBJECTION (réouverture n°10) : **retirée** — scène figée se prouve sur `texte_ouverture_joueur`, champ déjà livré ; aucun champ neuf, REPORTÉ n°10 intact.
- RISQUE (2 branches sans ancrage) : **maintenu, corrigé** à 1 branche (relation secrète) après convergence grep ; antagoniste rejoint le report par mon propre arbitrage de valeur, pas par manque d'ancrage.
- PROPOSITION (téléchargement → n°2 nommé) : **maintenue**, consensus confirmé.
- VERDICT : conditionnel levé → **feu vert**, sous 2 conditions désormais réglées avant code : (1) critère 9 réécrit à 7 branches testées (retrait antagoniste + relation secrète, reformulation scène figée/jalon/événement), avec renvoi explicite n°4 pour les deux retirées ; (2) aucune ligne sur `budget-injectable` dans le code d'it5.
