RISQUE — AC#9 nomme 9 branches, mais un grep exhaustif de `types.ts` (« antagoniste », « relation », « obligatoire », « lié…histoire », « figée ») ne retourne **aucun** champ pour : antagoniste, relation secrète, jalon non obligatoire, événement non lié à l'histoire — en plus de « scène figée », déjà connue reportée n°10. C'est 5 branches sur 9 sans ancrage structurel. Un test écrit contre elles serait soit un contrôle à l'œil (interdit KR-157), soit inventerait un champ hors décision A — qui réserve la forme complète de `personnages`/`evenements` à leur feature éditrice, pas à dossier-format.

OBJECTION — le libellé du critère n°9 dans la spec (ligne 27) dit encore « delta sur chaque cible », sans « ADMISSIBLE », alors que `resolved_decisions` d'it4 (ligne 409) a déjà tranché « sur chaque cible ADMISSIBLE » avec son motif (climat sans delta admissible). Décalage confirmé, pas supposé.

PROPOSITION — avant code : (1) corriger le critère n°9 en « … ADMISSIBLE » ; (2) ancrer chaque branche restante sur un champ nommé : `portee='second'` (Personnage.portee), `fin avec …_expr` (Fin.condition_expr), `savoir avec porte apres_indice` (Savoir.revele_si.apres_indice_id) ; « jalon non obligatoire » → reformuler « jalon sans `declencheur_expr` » (champ optionnel existant) ; « événement non lié à l'histoire » → « événement sans `declencheur_texte` ni `declencheur_expr` » ; (3) « antagoniste » et « relation secrète » : retirer de la checklist d'it5, reporter nommément à la feature qui édite `personnages`, au même titre que `contre_mesures` déjà reporté. Sur le téléchargement (point 6) : si retenu ici, son critère doit être prouvé à l'échelle brain/ (appel `exportDossier`→`downloadJson` vérifié sans montage d'écran complet), jamais par la seule présence du bouton — même doctrine que KR-156 pour l'import.

VERDICT — veto sous réserve : recevable seulement si le critère n°9 est réécrit branche par branche avant code, chaque branche portant soit un champ existant nommé, soit un retrait/report explicite.
