RISQUE — La couche validateur (Delta[] sur monde.quetes[].recompense) est déjà lourdement couverte : confirmé en lisant tables.ts (CHEMINS_DE_DELTAS l.575, aucune ligne monde.quetes[] dans CHAMPS_REQUIS) et validate.test.ts (bloc CAS_DE_DELTAS l.1421-1690+, ~10 tests nommés dont « quetes recompense », déjà verts). Le vrai risque est ailleurs : EditeurEffets est un composant NEUF sans aucun précédent testé — cible orpheline, doublon, liste vide n'ont aucun test nommé dans le cadrage.

OBJECTION — EtapeQuete.libelle (string, non optionnel au type) n'a PAS de statut CHAMPS_REQUIS tranché par le cadrage (L4 : « SI libelle devient requis »). Or KR-214 impose que cette décision précède le choix du geste d'ajout (commit immédiat vs brouillon différé façon useEcriturePlan.ts). Un plan qui code l'écran avant de trancher ce point est une définition de fini floue.

PROPOSITION — (1) Non-régression : les tests existants nommés « une chaine au chemin de delta quetes recompense est refusee », « un chemin de delta quetes recompense absent est bloquant », « un objet quelconque aux quatre chemins de delta est desormais refuse » restent verts, sans modification. (2) Test nommé « une cible orpheline de récompense s'affiche via avecOrpheline(), jamais retirée silencieusement » (précédent FicheIndice). (3) Test nommé « deux effets identiques dans recompense[] sont acceptés et rendus en deux lignes distinctes » (doublons autorisés à trancher explicitement). (4) Test nommé « une quête sans récompense affiche l'état vide dédié, jamais un vide silencieux ». (5) Le plan tranche par écrit le statut CHAMPS_REQUIS d'etapes[].libelle avant le lot écran.

VERDICT — recevable sous réserve (statut CHAMPS_REQUIS d'etapes[].libelle explicité + les 3 tests UI nommés ci-dessus ajoutés au plan).
