RISQUE — Le cadrage laisse deux points non tranchés qui, non résolus avant l'écriture du lot contrat, reproduisent exactement le trou qu'il0 a lui-même documenté à l'itération 2 (KR-214) : (a) `EtapeQuete.libelle` est un précédent connu (`PlanAction.action`) mais le cadrage ne dit PAS s'il est requis par élément — s'il l'est, le geste d'ajout immédiat cesse de s'appliquer à la liste imbriquée même si `Quete` elle-même n'a aucun `CHAMPS_REQUIS` ; (b) le cadrage présente `EditeurEffets` en partie comme un livrable d'architecture (« réutilisé sans fork par it4 ») plutôt que comme la simple UI du champ `recompense` déjà existant — à surveiller pour que la réutilisabilité ne pousse pas le lot écran au-delà d'une fiche de quête démontrable en une phrase.

OBJECTION — Le champ candidat `Quete.objectif` (L5) reprend littéralement le mot que KR-198 a proscrit précisément pour empêcher la confusion entre une RÉFÉRENCE (`objectif_id` → `canon.objectifs[]`) et une PROSE libre — décision déjà actée et documentée dans `types.ts:860` (« Clé `but`, JAMAIS `objectif` »). Le cadrage réintroduit le mot interdit sur une autre entité du même schéma sans le justifier ; un auteur qui relit son JSON (audience explicitement citée par ce docstring) retrouve la même ambiguïté que KR-198 a été écrit pour éliminer.

PROPOSITION — Renommer le champ candidat `objectif` → `consigne` (ce que le donneur demande, prose libre, zéro collision avec `objectif_id`/`canon.objectifs[]`) avant l'écriture du lot contrat. Trancher explicitement dans le plan d'it3 si `EtapeQuete.libelle` est requis par élément ou optionnel — décision à écrire noir sur blanc, pas à découvrir en cours de lot comme it2.

VERDICT — recevable sous réserve (renommage du champ + décision explicite sur `libelle` avant le lot contrat).
