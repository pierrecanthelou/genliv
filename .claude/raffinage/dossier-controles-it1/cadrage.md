# Cadrage — `dossier-controles` · itération 1 (le squelette)

Envoyé identique aux cinq rôles. Lecture seule ; seul l'orchestrateur écrit.

1. **Feature** `dossier-controles` (n° 7), cadrée le 2026-09-15 — spec : `src/features/dossier-controles/specification.json` (4 itérations, 13 `known_risks`, 11 `resolved_decisions`, 9 `open_questions`). À lire en entier avant de parler.
2. **`goal` brut (it1)** : poser `brain/dossier/controles.ts` — registre fermé de règles, rapport dérivé, seuil `jouable` — et l'écran Contrôles qui le rend, avec la première règle : les proses encore marquées `⟨à écrire⟩`.
3. **Phrase de démo** : « À la fin, l'auteur voit, dans un écran Contrôles, quelles proses de son dossier restent à rédiger. »
4. **La règle est SCINDÉE par AUDIENCE** (arbitrage de cadrage, non rouvrable) : **BLOQUANT** sur `charpente.depart.texte_ouverture_joueur` (audience `moteur`, lue au joueur mot pour mot) ; **ALERTE** sur les trois proses `ia` du canon (`synopsis_mj`, `accroche_joueur`, `ton`). Motif : aucune règle bloquante ne prend pour critère un champ d'audience `ia`.
5. **Matériau** : `brain/dossier/amorce.ts` sème ces quatre textes préfixés de `MARQUEUR_A_ECRIRE` (`⟨à écrire⟩`) ; son JSDoc nomme n° 7 comme consommateur attendu. La constante n'est **pas** au baril et **deux tests-grep** la gardent (KR-223) — `controles.ts` vit dans `brain/dossier/` et l'importe depuis `./amorce`, sans jamais recopier le glyphe.
6. **Décisions de cadrage qui s'appliquent et ne se rouvrent pas** : type **frère** `Controle`, jamais `'info'` ajouté à `DossierIssueSeverity` ; la règle **déclare** sa section (KR-219) ; anatomie à **trois lignes** calquée sur `IssueList` (KR-164) ; aucune ligne « réimportez-le » ; **pas de troisième couleur sémantique** ; le linter **se tait** sur les collections vides ; **aucun lot contrat** sur `types.ts` / `destinations.ts` / `validate.ts`.
7. **Hors périmètre, déjà affecté** : les badges de nav → it2 ; les cinq règles de registre et les avertissements du validateur → it3 ; l'atteignabilité, « canon sans objectif » → it4.
8. **Fichiers probables** : (N) `src/brain/dossier/controles.ts`, `controles.test.ts` · (R) `src/brain/index.ts` · (N) `src/features/dossier-controles/{index.ts, components/PanneauControles.tsx, components/ListeControles.tsx, tests/…}` · (R) `src/features/bascule-editeur/components/DossierEditorScreen.tsx`, `SectionNav.tsx` et leurs tests · (R) `src/App.tsx`.
9. **Point dur mesuré** : la section sélectionnée est un `useState<SectionId>` dans `DossierEditorScreen`, et `panneaux` est un `Partial<Record<SectionId, ReactNode>>` dont les **dix** emplacements sont déjà remplis par `App.tsx`. Une onzième destination « Contrôles », qui n'est pas un `SectionId`, oblige donc à élargir **cet état** et **ce contrat de prop**. À noter aussi : `PANNEAU_PAR_SECTION` (`PanneauSection.tsx`) est un `Record<SectionId, …>` exhaustif par compilation, et `IssueList` porte `maxHeight: 240` — un gabarit de modale, pas de panneau plein.
10. **Signal de coupe présent et assumé** : l'itération touche **deux** features (`dossier-controles` neuve + une adoption de quelques lignes dans `bascule-editeur`). L'orchestrateur ne coupe pas — un module `brain/` sans écran n'est pas une tranche verticale — mais si un rôle voit une découpe qui **garde la démo**, qu'il la propose.

**Composition : 5 rôles.** Motif : l'arbitrage central de l'itération est une question d'**audience** (`moteur` contre `ia`), et la leçon de `dossier-registres` élargit nommément le critère de convocation de `narratif-ia` à toute itération qui raisonne sur les destinations, pas seulement à celles qui touchent un prompt.
