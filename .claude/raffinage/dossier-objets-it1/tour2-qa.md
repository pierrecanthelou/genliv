# Tour 2 — QA — `dossier-objets` it1

**Veto retiré** — motif : la convergence indépendante des 3 rôles sur boutons Monter/Descendre (frères de `ListRow`, hors `<button>` imbriqué) rend le critère observable au niveau composant : `getByRole('button', {name:/^Monter/})` → `user-event.click` → assertion sur `monde.objets` (ordre) et sur l'argument passé à `dossiers.update`. Ça satisfait ma propre proposition de tour 1.

**Réserve maintenue sur le niveau exact** — un `user-event.click` prouve l'action, pas l'opérabilité clavier. Vu que c'est précisément ce qui a déclenché la convergence de ce tour (UX : « sans équivalent clavier nulle part »), le critère #3 doit nommer explicitement DEUX assertions dans le même test, pas une seule qu'on suppose transférable : (1) clic, (2) `Tab` jusqu'au bouton + `{Enter}` (`user-event.keyboard`) produit le même effet. Sans la seconde, « opérable clavier » reste une affirmation non vérifiée par un test qui ne clique qu'à la souris.

**Découpage ListRow — position QA** : la proposition Tech Lead (zéro fichier `brain/` modifié, boutons rendus par `PanneauObjets`) est préférable pour KR-199. Le test à discriminance (2 objets distincts, l'un monté/descendu, l'autre non affecté) ne vit alors que dans `panneauObjets.test.tsx`, un seul rendu, un seul brouillon d'état — pas de couplage entre une suite `listRow.test.tsx` (qui devrait mocker `onReorder`, donc ne prouve rien sur `monde.objets` réel) et une suite `PanneauObjets`. Le découpage UX (props sur `ListRow`) oblige à répartir la preuve : `ListRow.test.tsx` prouverait le callback isolé (sans document réel), `PanneauObjets.test.tsx` re-prouverait l'ordre — deux tests dont le second seul porte la garde KR-199, le premier illusoire. Plus de surface = plus de risque KR-199 (nom > assertions).
