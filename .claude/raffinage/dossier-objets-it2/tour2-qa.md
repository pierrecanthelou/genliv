# Raffinage `dossier-objets` it2 — Tour 2 (QA)

**Désaccord maintenu avec Tech Lead**, motif précis vérifié dans le dépôt : j'ai grepé l'usage de `CHEMIN_REFERENCE`/`dossier-reference.json` dans les features soeurs (`panneauPersonnages.test.tsx`, `savoirs.test.tsx`, `panneauObjets.test.tsx`). **Dans les trois, sans exception, ce fichier ne sert qu'à UN seul test — le non-régression structurel (AC22-équivalent, « dossier accepté, champs intacts »).** Tous les tests de refus/discriminance (KR-197/199, et le `retraitPersonnage.test.tsx` que j'ai lu en entier au tour 1) sèment des entités locales fraîches (ALDUR/SELENE/BREWAN), jamais un id tiré de la référence. C'est le patron établi du dépôt, y compris dans dossier-fiches it7 — cité au cadrage comme « même patron ».

Motif QA, pas de goût : `dossier-reference.json` a plusieurs écrivains non coordonnés (chaque feature y ajoute sa propre non-régression AC22). Si un futur lot y touche `pnj.tobin-le-gamin.savoirs[0]` pour SA propre AC, le test de discriminance d'it2 casserait sans rapport avec dossier-objets — échec silencieux, diagnostic à distance. Une fixture locale au test ne bouge que si CE test change. Je maintiens ma proposition : fixture 2 objets, locale au fichier de test d'it2, `dossier-reference.json` non touché même en lecture pour ce test-là.

**Armement (bouton actif + spy + message verbatim) : position inchangée, complétée.** Vu la mention `forwardRef<FicheObjetHandle>` de l'UX (bon choix, évite BUG-078) : le focus post-retrait doit être prouvé par la sonde à candidats multiples (`querySelectorAll('button[aria-label^="Retirer"]').length > 1`), pas un simple `toHaveFocus()` — précédent KR-199, sinon un mutant sur l'index survit.
