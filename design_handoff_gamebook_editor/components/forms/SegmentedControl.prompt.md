Inline tab switcher on a sunken track — the action-type selector and the Prendre/Écouter/Fouiller sub-modes. Active segment fills dark.

```jsx
<SegmentedControl
	value={action}
	onChange={setAction}
	options={[{value:'pnj',label:'PNJ'},{value:'decor',label:'Décor'},{value:'monstre',label:'Monstre'}]}
/>
```
