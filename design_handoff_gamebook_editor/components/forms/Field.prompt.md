The base authoring input — labelled box for a node's text fields (nom interne, description joueur, libellé de choix, dialogue).

```jsx
<Field label="DESCRIPTION" hint="lue par le joueur" multiline rows={3} value={desc} onChange={onDesc} />
```
