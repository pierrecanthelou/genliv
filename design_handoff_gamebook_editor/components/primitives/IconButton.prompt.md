Compact square button for row actions — edit (✎), delete (✕), add (+).

```jsx
<IconButton tone="default" label="Éditer">✎</IconButton>
<IconButton tone="danger" label="Supprimer">✕</IconButton>
```

Tone `danger` colors the glyph terracotta; `accent` fills with blue (active state).
