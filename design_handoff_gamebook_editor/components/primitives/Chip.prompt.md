Rounded token for inventory items, required objects, and source chips — removable when editing a list.

```jsx
<Chip tone="neutral" onRemove={() => drop(id)}>Gourdin clouté</Chip>
<Chip dashed>+ objet</Chip>
```
