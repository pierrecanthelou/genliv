Standard card header — mono eyebrow + Hanken title, optional trailing action. Use atop every dashboard/editor card.

```jsx
<CardHead eyebrow="DÉCOR · PRENDRE" title="La salle aux runes" trailing={<IconButton label="Agrandir">⤢</IconButton>} />
```
